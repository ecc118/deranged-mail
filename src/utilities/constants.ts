export const MESSAGES_LIMIT = 10;

export const MAX_COMPRESSED_SIZE = 700;

export const IMAGE_COMPRESSION_QUALITY = 0.5;

export const MAX_CHARS_NOTIFICATION_BODY = 40;

export enum NOTIFICATION_BODY {
  IMAGE = 'sent an image',
  VIDEO = 'sent a video',
}
